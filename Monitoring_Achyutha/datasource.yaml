apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    uid: eegzltfarvi0wb
    url: http://prometheus:9090
    jsonData:
      httpMethod: GET
      timeInterval: 15s
    editable: true
    isDefault: true
